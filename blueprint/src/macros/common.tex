% In this file you should put all LaTeX macros and settings to be used both by
% the pdf version and the web version.
% This should be most of your macros.

% Theorem-like environments are NOT defined here: they come verbatim from the
% master file's preamble via masterdefs.tex (auto-generated by
% scripts/extract_blueprint_content.py from Octonionic_RH_master.tex).
% The master defines: theorem, proposition, lemma, corollary, definition,
% condition, remark, example -- all sharing the theorem counter, numbered
% within sections.