# Concentricity over the octonions — proof outline

## The theorem

```lean
ASection.concentricity (A : ASection) :
  ∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c
```

## One infinite analytic subject

C1--C4 jointly determine the subject:

- continuation through the real pole and common north pole;
- the infinite Euler presentation;
- the infinite Weierstrass presentation of the same factor;
- the infinite residue-`ℂ` zero family.

The prime-indexed GPV lift, winding, real levels, Euler and Weierstrass
presentations, and north behavior form one vertically integrated action.

Its distinguished element is simultaneously the slice-preserving function
and the diagonal Möbius group element fixing `0` and `N`. Because it is a
function, its domain and codomain value states are intrinsic evaluations.

## Input and global geometry

For `s ∈ 𝕆*`, normalization `(dir s, sliceCoord s)` supplies the sphere and
coordinate on which the already-complete distinguished element acts.

Orbit--stabilizer positions this same element in every projective frame and
makes its action functorial on objects and morphisms. It acts uniformly
throughout the continuum of Riemann spheres. `G₂` supplies the natural
isomorphisms between the directions in `SphereWorld`.

The old `AsectionSlice : GreatCircle.Base ⥤ SphereWorld` is one sectional
projection. It does not carry all normalized inputs or their values.

At each projective frame `X`, the completed action produces:

```lean
Grpd.of
  (AsectionActionStateWorld A (projectiveObjectFrame A X)).
```

This is the groupoid of all zero and nonzero intrinsic value states
generated by the distinguished element at `X`. `AsectionActionDiagram A`
then organizes these groupoids and their invertible transports over the
projective base.

The existing `AsectionEquivariant A : H1 ⥤ H1` is the point-valued
round-trip eye. Gate 1 must certify its exact terminal comparison with the
completed action.

## Downstream reading

After the certified global action and Grothendieck total:

1. retain the certified semantic `CResidueZeroLocus A`;
2. form and triple-certify its framewise full inverse-image groupoid;
3. prove whole-action preservation for every base arrow and restrict the
   existing transports, producing the C-residue diagram and its natural
   inclusion;
4. totalize that inclusion over the same projective base;
5. name and triple-certify the exact residue orbit, stabilizer, coset, and
   component objects;
6. recognize through the vertical/horizontal first-isomorphism and
   orbit–stabilizer comparisons that the complete residue total is the
   action groupoid already built, then apply Remark 8.3.5 and the
   category-of-elements identity to its categorical one-component
   calculation;
7. apply the separate Grothendieck-components equivalence at the residue
   diagram last;
8. descend the action's already-compatible `ℝ`-valued real-level orbit
   invariant to `val`, keeping its unique class visible;
9. define `c`, prove the atomic common-centre statement, then discharge
   corollaries and the terminal axiom audit.

The residue system, descended reader, and centre are outputs. None is an
input field or independently populated object. C4 proves infinitude. The
certified inverse image already has inhabitants, and the categorical
one-component calculation comes from the action groupoid and
orbit–stabilizer factorization. No topological connectedness statement is
used.

## Status

- analytic and geometric suppliers: extensively green;
- exact Gate-1 unity and terminal comparison: certified;
- Gate 2, the canonical `TotalActionStateWorld A` Grothendieck
  construction: certified;
- the exact framewise `F_A(X)` and semantic `CResidueZeroLocus A`:
  certified;
- the framewise inverse-image groupoid: certified;
- whole-action preservation making those certified fibres a diagram: next;
- all subsequent gates: held.
