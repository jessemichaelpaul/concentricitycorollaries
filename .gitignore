# Lean / lake build artifacts and downloaded dependencies (regenerated by
# `lake build` / `lake exe cache get`)
/.lake

# macOS noise
.DS_Store

# Claude Code local settings
.claude/

# Local agent sandbox configuration is retained on this machine but is not
# part of the public mathematical source tree.
/.codex/

# Retired/generated publication surfaces are not part of the source-centered
# public repository. `Octonionic_RH_master.tex` is the prose source of truth.
/blueprint/
/docs/
/web/
/home_page/
/output/
/.attic_old_bases/
/archive/
/Octonionic_RH_master.pdf
/rebuild.sh
/scripts/extract_blueprint_content.py
/.githooks/
retired/
/tmp/

# Public Markdown policy: the project overview and the Haskell program guide.
# Local plans, transcripts, and agent instructions are not publication files.
*.md
!/README.md
!/tools/semantic-harness/README.md

# Reading-room and personal working material stays local.
/inbox/
/JMM2027_abstract_template.tex
/Jesse_Plan_of_Study_FA2025_updated.pdf

# This is useful local evidence for checkpoint 1, but it is not imported by
# the production library. Publish it only if it is integrated deliberately.
/Concentricity/ASectionActionCorrespondence.lean

# LaTeX build artifacts (regenerated by latexmk/pdflatex)
*.aux
*.log
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.nav
*.snm
*.vrb
