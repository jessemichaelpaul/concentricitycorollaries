# Codex operating register — Concentricity

Load `$concentricity-functorial-register` and read:

1. `register/00-register.md` — epigraph, invariant, one failure mode, routing;
2. `CURRENT_GATE1_MEMORY.md`;
3. `CURRENT_GATE1_HANDOFF.md`;
4. `EndgamePlan.md`.

`register/` is the shared register, tracked in git and read by Claude and
Codex alike. The skill is a loader that points there. Do not keep a separate
installed copy of the register and do not paraphrase it into task
instructions — that divergence is what produced the conflicting plans.

Do not reconstruct the architecture from historical tasks.

## Active task

Gates 1 and 2 are certified. The canonical top-level total is
`TotalActionStateWorld A`. The framewise value `F_A(X)` and
`CResidueZeroLocus A` are certified. The framewise inverse-image groupoid is
also triple-certified. Whole-action preservation is the next open theorem;
it then makes those already-certified fibres a diagram. The remaining
readout and corollary steps form the unified endgame gate, with internal
kernel checkpoints governed by `register/70-whole-square.md`.

The exact all-`t` supplier is the green
`reindexAsectionPresentation A f`, with its identity and composition laws.
The accepted state transport is `AsectionActionTransport A f`. The sole
open receipt is their comparison at
`InverseImageCResidueStateWorldGroupoid A X` and its target, packaged
directly as `cResidue_preserved`. It is not another action or another gate.
After it, `ObjectProperty.lift` and `liftCompιIso` force the restricted
arrow and inclusion square.

C1--C4 determine one infinite, vertically integrated function/Möbius
element. Normalized `s ∈ 𝕆*` is input to it. Orbit--stabilizer transports it
uniformly through projective objects and arrows; `G₂` connects the continuum
of sphere directions in `SphereWorld`; its input and output states are
intrinsic evaluations.

`AsectionSlice` is a sectional projection. The action is not semantically
constant. At frame `X`, name the native Gate-1 object first as
`Grpd.of (AsectionActionStateWorld A (projectiveObjectFrame A X))`.
`AsectionActionDiagram A` organizes those element-generated groupoids; it
is not a generic wrapper.

`AsectionEquivariant A : H1 ⥤ H1` is a genuine function eye. Its existence
does not by itself close Gate 1.

## Stop conditions

Stop if:

- a generic carrier, wrapper, cone, product, or index is being designed;
- a chosen sphere replaces the full `SphereWorld`;
- a local square is promoted to the whole action;
- values, zeros, cone legs, labels, or a reader are independently supplied;
- downstream category theory is used to redesign the upstream action;
- a missing name is reported as missing mathematics.

Do not edit Lean for the current residue-subdiagram gate until its exact
framewise inverse-image type and whole-action preservation walk-around have
been approved. Do not edit the master TeX during this Lean phase.
