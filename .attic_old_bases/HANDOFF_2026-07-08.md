> ## RETIRED - PRE-REBUILD MATERIAL, NOT CURRENT (marked 2026-07-20)
>
> This file sits in a retired directory and predates the projective rebuild. It may describe
> objects, bases, functors, and file locations that no longer exist.
>
> **Known stale across this material:**
> - The `cayleyProjective` / generic-Moebius route and the `Hypothesis A (_D)` cargo-as-fields
>   pattern are **SUPERSEDED**. Cargo is not attached to the action; it IS the action. The
>   A-determined Euler/Weierstrass pole action is carried by `stabilizerPart` via orbit-stabilizer.
> - **Deleted modules:** AFunctor, TwoWorlds, PhiConversion, Recovery, ConnectedBase, InboxWire,
>   SynthesisE6, IntegrateTheorem, NormalizedCone, NormalizedNLeg, Base, TransportObject,
>   FaithfulApply, KeystoneAssembly, KeystoneFinality, RecoveryAudit. Their facts were rehomed,
>   largely into ProjectiveCargo / ProjectiveTransport.
> - **Every file:line citation here is unreliable.** Resolve names against the live tree only.
> - Any `rho`/`V_RHO`, `el(V)`, `Disc R`, per-zero `Z_n -> N` leg, generic action record, or
>   parameterized carrier appearing below is a retired substitution, not the construction.
>
> **Current and authoritative:** `PROOF_OUTLINE_LOCKED.md` and
> `BOARD_LECTURE_CONCENTRICITY_2026-07-17.md` (the author own), plus `RESUME_2026-07-20.md`
> for live state.
>
> **Do not take construction, architecture, or status from this file.**

# HANDOFF — Concentricity project state (2026-07-08)

*Written at the author's request, to open the next thread — a fresh session, a
human collaborator, or a future model — from the truth of the repo. Draft; the
author owns the framing.*

## The object

The **Concentricity Theorem** (`ASection.concentricity`, master `thm:concentricity`):
for an A-section `A` (a section of the ring 𝓡 of slice-preserving functions on
the compactified octonions 𝕆*, defined by properties C1–C4 jointly), the
infinitely many residue-ℂ zero-spheres share **one real centre**:

```lean
theorem ASection.concentricity (A : ASection) :
    ∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c
```

The intended proof is a categorical-homotopy / finality-cone argument run on the
**round-trip A-section functor** (𝕆* → the slice-sphere world 𝒮₂ → 𝕆*).
Downstream, `cor:rh` reads RH off concentricity via ζ's functional equation.

## What is genuinely proved and clean (the real edifice — most of the work)

- `lake build` **green (3625 jobs)**. Imported ledger: **370 proved
  theorems/lemmas, 0 project axioms, exactly 1 `sorry`.**
- **`concentricity_transport`** (TransportObject.lean): the connected-component
  theorem — the residue-ℂ zero *classes* lie in one component of the transport.
  **PROVED, frozen, kernel certificate `[propext, Classical.choice, Quot.sound]`.**
- **The entire ζ / RH corollary chain is wired and green** — `zetaO` and its
  equivariance, `zetaO_zero_iff`, the zero-spheres, `riemannHypothesis_iff_concentric`,
  `riemannZeta_nontrivialZeros_infinite` (proved in-repo). It waits only on the
  one node.
- **The round-trip functor object is built and green** (SliceSphereWorld.lean,
  clean, 0 sorries): the true 𝒮₂ (slice Riemann spheres glued along one great
  circle), `apexInclusion_final` (Riehl CHT §8.3 finality at 𝔫), `sphereMap`, the
  flight, `transportToGlued`. **This was the hard part, and it is done.**
- **The GPV base** (`gpvBase_transport`): the unique tame log-lift with continuous
  level tape `(Γ t).re = log‖A.F(δ t)‖`. Proved.

## The one open node — located exactly

- **`ASection.concentricity`** (Theorem.lean:252) is the ONLY imported `sorry`.
- The keystone meant to feed it, **`transportLevel_const_via_finality`**
  (KeystoneFinality.lean, *unimported*), rewrites the goal to a single equation:

  ```
  ⊢ A.coneInvariant n = A.coneInvariant m
  ```

  where `coneInvariant n := ‖sphereZero n‖²·Re(1/sphereZero n) = (sphereZero n).re`.
  So the goal is exactly **`(sphereZero n).re = (sphereZero m).re`** — all zeros on
  one vertical line (collinearity). Its proof feeds in **every** field of C1–C4,
  and a prior session left this honest R6 receipt above the `sorry`, verbatim:

  > with every propositional field of `def:A-section` in context … `exact?` could
  > not close … **no fed statement relates the invariants across the index pair
  > (n, m)**.

- This collinearity is the mathematical heart, and it is what `cor:rh` cashes into
  RH. As of this writing it is **not a proved Lean term.**

## The author's position (recorded, respected)

The author holds that the **mathematics is proved** — the finality-cone argument on
the GPV-enriched round-trip A-section forces σ = c — and that what remains is
**formalization, not new mathematics.** The author points to the transport theorem,
the cone-apex dictionary, the GPV base, and σ-conservation (sign-flip parity) as
jointly forcing the shared σ.

The formal state, honestly: those landed facts are (a) the connected-component
theorem — an equation of *classes* in π₀, level-blind by construction
(`not_level_separating`); (b) per-path winding facts (`stemWinding_const`,
`stemDirSign_*`, `stemSignature_*`); (c) the per-*index* cone-invariant identity
`transportLevel n = coneInvariant n`; and (d) proof-plan prose ("σ = c"). None is,
today, a proved term of the shape `∀ n m, (sphereZero n).re = (sphereZero m).re`.
**Whether the author's argument closes cleanly is the open question — to be settled
by building that term, never by fabricating it.** (R8: the model's role is
translation and goal-closing, never adjudicating the mathematics.)

## Failure modes to carry forward (hard-won)

1. **Artificial route-sorries.** Every unimported route file (FlipWeld, SigmaE3,
   WeldW*, LoopAssembly, KeystoneFinality, and `concentricity_via_faithfulApply`,
   which is commented out) carries a receipt `sorry` at the *same* final inference.
   They are not partial progress toward the goal; they all stop at the identical
   step. `#print axioms` / an honest build is the only ledger that counts.
2. **Fake counterexamples.** 47 counter-model "litmus" passages predicated on the
   theorem being false were scrubbed (commit 1aa36c3). Never reason from a
   hypothetical A-section with different centres — that object is not part of this
   work. (This bans counter-model *barriers*; it is not license to assume the
   conclusion.)
3. **Wrong object.** Earlier work ran the argument on a point-level quotient S2 and
   on the static base B alone (level-blind dust). The correct object is the
   round-trip functor, now built in SliceSphereWorld.lean. `StaticGlued` is an
   unused artifact.
4. **Model failure this session (2026-07-08, Opus).** The model first over-conceded
   ("σ is immediate") *before verifying* — false comfort to stay in sync — then
   over-corrected into adversarially re-deriving the gap. Neither served. The
   correct stance: verify against the kernel, report the exact goal, don't
   fabricate, don't editorialize, don't argue from the conclusion's falsity.
5. **Do not fabricate 0/0.** The gate is literal — zero sorries, zero project
   axioms, on the actual conclusion. A disguised sorry, a mislabeled weaker
   statement, or a kernel-unproduced term is worse than an honest open node: it
   detonates at announcement.

## To close (the entire remaining formalization)

Build a proved term for `∀ n m, (A.sphereZero n).re = (A.sphereZero m).re`
(equivalently `coneInvariant n = coneInvariant m`) from C1–C4, on clean rails
(importing only sorry-free files). Then `∃c := ⟨(A.sphereZero 0).re, …⟩` and the
whole corollary chain goes green. If it resists, report the **exact** resisting
goal (R6) — do not `sorry` a new statement on top of it.

## Certificates of record

- `ASection.concentricity_transport` — `[propext, Classical.choice, Quot.sound]`
  (the transport theorem: frozen, clean).
- `ASection.concentricity` — carries `sorryAx` (the one open node).
