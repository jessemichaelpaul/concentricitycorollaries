# SUPERSEDED FINAL GUARDRAIL — 2026-07-17

This former execution guardrail is retained only as a redirect. Its old
"analytic cargo," bulk-transfer, and hand-built zero-to-$N$ language is not an
instruction and must not be used.

The current binding construction plan is:

- `PLAN_TWELVE_ON_THE_DISK_ACTION_2026-07-22.md`;
- `HANDOFF.md`;
- the live Lean definitions.

The A-section functor has exactly this type:

```lean
sectionFunctor A : GreatCircle.Base ⥤ SphereWorld
```

C1–C4/W/GPV determine the complete function-valued disk action. Each
load-bearing fact is proved natively on that action and immediately extended
wholesale by orbit–stabilizer in its own pass. The locked order is

```text
4 -> 5 -> 6 -> 1 -> 2 -> 3 -> 7 -> 8 -> 9 -> 10 -> 12
-> sectionFunctor acceptance -> exact 𝒯_A -> 11
-> 8.3.4 -> π₀ -> labelCocone -> val -> ∃ c
```

No projected or intermediary functor, generic bundled-groupoid codomain,
wrapper, theorem-field specification, per-index connector, or downstream
naturality obligation may define the A-section action.

Never construct a naturality cone. The completed functor and exact total make
the pull to the common witness `N` intrinsic, and the readout detects it.
