# RESUME — Concentricity, 2026-07-20 (paused mid Phase 1)

**Mathematical author: Jesse Michael Paul.** Roles: Jesse = authority; Codex = sole Lean editor;
Opus = read-only auditor. Written at pause for battery; nothing was in flight.

---

## 1. Verified state at pause (Phase 0, green)

- Full build **green: 3683 jobs**. HEAD `7918d66`. No source edit pending.
- `CayleyDictionary.lean` **identical to HEAD and rebuilt successfully** — the author's
  distinguished element is intact and compiler-verified.
- Only two executable `sorry` terms in the tree:
  - `ASection.concentricityReadout`
  - `ASection.concentricity`
- `concentricity_via_weldW3` is **already absent** — nothing to delete under that name.

**Green and universal (the vehicle, do not rebuild — verify only):**
`orbitRep_spec` (N reaches every base point) · `stabilizerPart` (proved to fix N) ·
`orbit_stabilizer_factor` (every arrow `= orbitRep b' · stabilizerPart f · (orbitRep b)⁻¹`) ·
`stabilizerPart_id = 1` · `stabilizerPart_comp` (residuals compose; orbit reps telescope).
All universal in `b, b', b'', f, g`. **Any action at N extends to a functor on the whole base.**

**The substitution still in place (Phase 1 removes it):**
`cayleyProjective` drives `projectiveConnection` (3 occurrences, `ProjectiveSection:271/278/279`) ·
`distinguishedWorldAction (m : Moebius)` `:149` and `distinguishedStateAction (m : Moebius)` `:187`
are **A-free generic families** · `Hypothesis A (_D)` attaches the twelve afterward, `_D` unused in
every field type.

**Already correct, needs no separate construction:**
`TotalA (A) := (sectionFunctor A).Total := Grothendieck (S.toFunctor ⋙ Grpd.forgetToCat)` —
defined *in terms of* the functor, so the repair carries it. Verify, do not duplicate.
`zeroTotal A n I` exists. Engine green: `pi0_grothendieck`, `toColimitObj`,
`toColimitObj_eq_of_zigzag`, `colimit.desc`.

---

## 2. Phase 1 — the atomic replacement

**The action is DETERMINED by C2/C3, not to be supplied, designed, or chosen:**

    μ_A(z) = A.F z = exp (∑' p, ℓ p z)      (C2, nonvanishing)
    acts by  q ↦ μ_A(z) · q                  (identity at multiplier 1; composition by multiplication)
    C3 is that same action continued through N.

Swap it into `map`, carried by `stabilizerPart f`. Atomic: the shortcut, the generic families, and
`Hypothesis A (_D)` come out in the same edit that puts the action in.

**The twelve are consequences, not inputs.** They are properties of that one action, holding of
every transport in every `I ∈ S⁶` simultaneously — nothing re-proved per point, arrow, or world.
No fields, no records, no compatibility layer.

**How Lean "knows" the twelve reach everywhere — definitional unfolding.** Once `A`'s exponential
is in the `map` body, every goal about an arrow of `A.TotalA` unfolds to mention `μ_A`, and the
twelve are theorems *about that expression* — so they apply by ordinary rewriting. Under the
shortcut the goal unfolded to an arbitrary Möbius element with no `A` in it, so nothing could
unify; that unification failure was repeatedly misdiagnosed as "a missing lemma." Making the
twelve *fields* of a structure does not help: a hypothesis in context does not put `A` into the
goal. That is why `_D` occurring in no field type was diagnostic.

---

## 3. Acceptance checks (greps + build; run after Phase 1)

    grep -c "cayleyProjective" Concentricity/ProjectiveSection.lean          -> 0
    grep -c "import Concentricity.CayleyDictionary" .../ProjectiveSection    -> 0
    grep -c "structure Hypothesis" .../ProjectiveSection.lean                -> 0   (removed, not renamed)
    distinguishedWorldAction / distinguishedStateAction (m : Moebius)        -> gone, not bypassed
    map body mentions A's exponential AND stabilizerPart                     -> yes
    no new structure with an action-shaped field; no `∀ m :` / `∀ R :`       -> yes
    orbit_stabilizer_factor / stabilizerPart consumed outside their block    -> yes
    HONESTY GUARD: deleting the exponential term makes the FUNCTOR fail to elaborate

**Green does not certify the statement.** Order at the end is always:
`#print ASection.concentricity` — read it, confirm it is literally
`∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c` — **then** `#print axioms`, expecting
`[propext, Classical.choice, Quot.sound]`. A clean axiom report was once produced for a theorem
whose type did not contain `∃ c`.

---

## 4. Phase 3 — the argument (after Phase 1 + total verified)

1. `zeroTotal A n I` — verify it still elaborates on the repaired functor.
2. `concentricityReadout A : ∃ κ : A.ValueColimit, ∀ n I, toColimitObj F_A (zeroTotal A n I) = κ`
   — already declared with the right type, currently sorried. Close by
   `toColimitObj_eq_of_zigzag` on the functor's **own** through-N transports.
   No hunted arrow, private north, pairwise `re`-equality, `IsConnected`, populated subcategory.
3. `labelCocone A` — its naturality **is** the action's own value preservation, already true at
   every arrow because every arrow is that action. Not a fresh lemma; not
   `constant_of_preserves_morphisms`.
4. `val A := colimit.desc P_A (labelCocone A)`; `val_zeroTotal` — read, not computed.
5. `concentricity` — atomic calc, replacing the winding body and both sorries.

**Stop rule:** a term that will not elaborate -> report the exact unsatisfied Lean type and stop.
Not missing mathematics, not an input request, not a new carrier.

---

## 5. Provenance warnings

- **`.attic_markdowns/DIRECT_A_FUNCTOR_FORMULAS_2026-07-16.md`** — the pre-shortcut construction,
  and its **architecture is right** (§4 "Orbit–stabilizer well-definition" over
  `MulAction.stabilizer GreatCircle.Aut ∞`; `:221` states the channels are *not generic*).
  **But its addresses are stale**, and its "carrier `NormalizedSlicePoint`" line is **struck** by
  the author's ruling of 2026-07-21 — that is the shortcut's carrier, not his; the two groupoids
  are `GreatCircle.Base` and `SphereWorld`, and `obj` is an output of the construction. `InboxWire`, `SynthesisE6`,
  `Recovery` are **deleted**; the facts were rehomed:
  `euler_branch_level` -> `ProjectiveCargo`; `zero_free_on_halfSpace` -> `ASection`/`ProjectiveCargo`;
  `normalizedZero_pole_*` -> `NormalizedPoleBridge`; `stemWinding_F_halfSpace` -> `WeldW12`.
  **Resolve every supplier by name against the live tree. Do not follow its file:line citations,
  and do not conclude a fact is missing because its cited file is gone.**
- **`ExecutionProbe.lean`** (deleted) was the rejected parameter-hunting route. Do not revive.
- Seven markdown documents carry correction banners; `PROOF_OUTLINE_LOCKED.md` and
  `BOARD_LECTURE_CONCENTRICITY_2026-07-17.md` are the author's own and are **correct**.
  Ruling B of `A_SECTION_FUNCTOR_BUILD_PLAN` and `FINALE_PREFLIGHT:55` ("cited theorem-fields")
  are **superseded** — cargo is not attached to the action; it *is* the action.

## 6. Auditor's edits today (verified, for the record)

- `G2.lean` — removed `def H1` + its `Groupoid` instance and rewrote the header. Nothing consumed
  `H1`. Author confirmed it is not needed.
- `NormalizedNLeg.lean` — deleted (per-zero `Z_n ⟶ N` attractor), plus its root import. Zero consumers.
- `CayleyDictionary.lean` — cut in error, **restored**; identical to HEAD and build-verified.

No other `.lean` file was touched by the auditor.
