> ## STATUS CLAIMS IN THIS FILE WERE NOT VERIFIED - corrected 2026-07-19
>
> This document asserted that the on-disk functor was built by the Moebius/**orbit-stabilizer**
> construction. **That was false**, and the same claim was repeated across several documents
> (AGENTS.md, A_SECTION_FUNCTOR_BUILD_PLAN, CONCENTRICITY_THEOREM_CLOSE_GUIDE,
> CONCENTRICITY_DOCUMENT_MAP, CARGO_INVENTORY, ZULIP_RELEASE_PLAN).
>
> Verified against the source, 2026-07-19:
>
>     grep -rn "orbit_stabilizer_factor" Concentricity/*.lean
>     # one hit: its own declaration, ProjectiveSection.lean:115. Consumed by nothing.
>
> `projectiveConnection`s `map` routed every base arrow through `cayleyProjective f.val` - a
> generic monoid hom into `Moebius` - bypassing `orbitRep` / `stabilizerPart` /
> `orbit_stabilizer_factor` entirely. `_D` occurred in none of the twelve `Hypothesis` field
> types, so the analytic cargo sat beside the functor rather than being carried by it.
> Repair to the author-s actual construction was begun the evening of 2026-07-19.
>
> **The author-s own specification is correct and takes precedence over this file:**
> PROOF_OUTLINE_LOCKED.md and BOARD_LECTURE_CONCENTRICITY_2026-07-17.md.
>
> **Rule going forward:** a status claim in a document is not evidence. Verify it against the
> source and record the command that verifies it.

# STEP-BY-STEP GUIDE — closing the Concentricity Theorem

**Mathematical author:** Jesse Michael Paul  
**Codex/Opus proof-transcription draft:** 2026-07-17  
**Author ratification:** RATIFIED, 2026-07-17  
**Status:** operational finale guide; Step 2 authorized

## The outcome

For every A-section functor, all infinitely many populated C-residue zero $6$-spheres share one
real value. In Lean the centre conclusion is

```lean
∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c
```

This must become a conjunct of `ASection.concentricity_theorem`; it is currently absent from
the theorem type. It may not be deferred to `ASection.nontrivial_one_centre`, a docstring, or
the zeta corollaries.

## Preconditions — all must already be green

Do not begin the finale until the exact printed interfaces show:

1. the general Möbius/orbit–stabilizer functor;
2. the author-ruled completed A-section interface built from that general action;
3. the Grothendieck total object of that exact completed functor;
4. the author-ruled C3/C4 zero-output family and exact C4 consequence;
5. the exact relation supplier and intrinsic real-value readout fixed in Build Plan Ruling D;
6. `pi0GrothendieckEquiv` specialized to that exact functor.

The present generic `projectiveSectionFunctor`/`Total` pair does not satisfy these preconditions
merely by retaining those names. Printed types decide.

## The two categorical facts

The finale uses two pieces of categorical homotopy theory.

### Readout

$$
\pi_0(\mathcal T_A)
\simeq
\operatorname*{colim}_{\mathcal B}(\pi_0\circ A).
$$

Riehl’s proof of Lemma 8.3.4 explains why the colimit relation is generated by arrows. The
project’s `pi0GrothendieckEquiv` certifies the readout in Lean. They are two descriptions of one
mechanism, not two sequential constructions.

### Singleton criterion

Riehl’s Remark 8.3.5 identifies connected components with objects modulo finite zigzags and
says that nonempty and connected is equivalent to having singleton $\pi_0$.

The proof does not construct pairwise zigzags. It applies this criterion to the globally
completed A-section transport system already carried by the functor and assembled in the
colimit.

## Step 1 — freeze the theorem type before proving it

Read the current source statement of `concentricity_theorem`, while recording that the dirty
module below the present `northWorldHom` arity mismatch is not a whole-module green
certificate. Preserve only analytic clauses whose last-green printed types
do not depend on the retired `PopulatedTransport`/`zeroZigzag`/`populatedHom` apparatus.
Restate the singleton clause solely over the completed cargo-bearing total and the canonical
C3/C4 output interface frozen by Step 8 of `A_SECTION_FUNCTOR_BUILD_PLAN_2026-07-17.md`. Add the
author’s conclusion as the final conjunct:

```lean
theorem concentricity_theorem (A : ASection) :
    CompletedTotalSingletonClause A
    ∧ RetainedPerZeroCargoClause A
    ∧ ∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c := by
  ...
```

`CompletedTotalSingletonClause` and `RetainedPerZeroCargoClause` here are schematic
descriptions, not new Lean aliases. Their actual types are frozen only after the construction
interface is printed. No clause whose dependency closure contains the rejected connector
apparatus is carried forward merely because it appears in the current theorem.

Conclusion Gate preflight:

- the printed proposition visibly contains `∃ c`;
- it quantifies over every enumerated C-residue sphere;
- it states equality of each sphere’s own real coordinate with the same $c$;
- it contains no numerical value such as $1/2$.

Stop for statement audit before writing the proof term.

## Step 2 — specialize the certified readout

Instantiate `pi0GrothendieckEquiv` with the exact completed A-section functor certified in the
construction phase. This step is conditional on the author-ratified functor type from Build Plan
Rulings A--B; the current projected general functor is not silently substituted.

The target is the single readout

```lean
ConnectedComponents (Total A) ≃
  Limits.colimit (((projectiveSectionFunctor A ⋙ Grpd.forgetToCat) ⋙ pi0Functor))
```

with all names unfolded far enough to confirm that both sides use the completed A-section
functor — `F.obj` and `F.map` both outputs of the orbit–stabilizer construction — and not the
earlier general projection or any independently selected carrier.

No category of elements is defined in the project. No comparison functor is added.

Readout gate:

- exact type printed;
- correct A-section functor on both sides;
- clean axiom report;
- no new theorem beyond specialization.

Stop for audit.

## Step 3 — identify the populated image wholesale

Map the C3/C4 output family into the colimit through the canonical Grothendieck/connected-
components cocone.

The completed interface must export the exact relation supplier fixed in Build Plan Ruling D.
That supplier records the A-section transports through the one common witness $N$. Riehl’s
colimit relation consumes all of those map-generated identifications at once. The exact
Ruling-C consequence of C4 supplies nonempty infinite population. This does not claim that the
current `projectiveSectionFunctor.map` already contains the analytic certificate; it does not.

This step is rejected if its proof term constructs or selects any of the following:

- a map from zero $n$ to zero $m$;
- a private north state for each $n$;
- a connector between two north states;
- a `Classical.choose` transporter;
- a manually composed zigzag;
- a pairwise equality of real parts.

The only permitted inputs are the completed interface’s author-ruled relation supplier, the
functor’s existing maps, the canonical colimit relation, and the exact C4 population result.

If Lean requests a statement that is not exported by the completed A-section interface, print
that exact requested type and stop. Do not fill it with the old `northWorldHom`, `zeroZigzag`,
or `populatedHom` apparatus.

## Step 4 — obtain the singleton class

Apply Riehl’s nonempty-connected/singleton criterion through the exact completed categorical
interface printed and frozen by Step 8 of `A_SECTION_FUNCTOR_BUILD_PLAN_2026-07-17.md`. Step 3
introduces no new populated category, subcategory, or replacement for the total object. Name
the unique class

$$
\kappa:\pi_0(\mathcal T_A).
$$

The corresponding Lean clause states that the range of the populated output family is the
singleton `{κ}`.

Singleton gate:

- the class lives in the connected components of the completed total object;
- population comes from C4 outputs;
- connectedness comes from the completed A-section map system;
- no generic base/fibre connectedness theorem discharges the goal;
- no hand-built connector occurs in the dependency closure.

Stop for audit.

## Step 5 — read the intrinsic real value

The objects identified in Step 4 are not mathematically value-free. Use exactly the intrinsic
real-value readout and compatibility supplier dictated in Build Plan Ruling D.

**Superseded sentence (2026-07-21):** this step previously observed that the
`NormalizedSlicePoint`/`NormalizedSliceHom` types "do not themselves contain such fields." Those
types were deleted on the author's instruction — they were the shortcut's carrier, not his
objects. The value is carried by the A-section functor's **own** transports, which are outputs of
the orbit–stabilizer construction along with its objects; the compatibility the cocone needs is
their real-value preservation, not a field added to a carrier.

Read that one value and call it $c\in\mathbb R$. Mathematically:

$$
\kappa=\{c\}.
$$

Then every populated zero output has real coordinate $c$:

```lean
∀ n : ℕ, (A.sphereZero n).re = c
```

This is a readout from the singleton’s own cargo. It is not:

- a projection newly defined after the colimit;
- a functor to `Disc ℝ`;
- a global preservation hypothesis;
- a theorem comparing pairs of zeros;
- a scalar attached to an abstract component.

Real-value gate:

- the exact intrinsic-value readout has Jesse’s ratified type;
- the exact compatibility supplier has Jesse’s ratified type;
- the proof cites those suppliers and the canonical singleton statement;
- removing the cargo would make this proof term ill-typed.

Stop for audit.

## Step 6 — close the integrated theorem

Assemble the three clauses already proved:

1. the populated singleton class;
2. the per-zero analytic content retained by the ratified completed interface;
3. the common-centre conclusion read in Step 5.

Do not simplify, rename, repackage, or move any clause. The theorem’s printed type is the
fidelity certificate.

Run the focused build. If it is green, immediately run:

```lean
#print ASection.concentricity_theorem
#print axioms ASection.concentricity_theorem
```

Expected axiom report:

```text
[propext, Classical.choice, Quot.sound]
```

There must be no `sorryAx`.

## Step 7 — terminal certificate and stop

Terminal green is reached only when the same report contains:

1. the successful build line;
2. the clean axiom print;
3. the full printed theorem statement containing the common-centre clause.

Then execute only the approved preservation actions:

1. present the three-part certificate to Jesse;
2. commit the exact terminal tree;
3. create the approved annotated tag;
4. create the approved bundle;
5. stop.

Do not clean up or improve the proof after terminal green. Repository cleanup and corollary
rewiring begin only in their separate approved phases.

## What the theorem says in ordinary language

The A-section functor carries one real value through the continuum of its slice worlds. Its
Grothendieck total object assembles all of those transports, and the colimit identifies the
populated degenerate-fibre outputs as one intrinsic real-value class. Consequently every
C-residue zero $6$-sphere output has the same real centre $c$. Since C4 supplies infinitely many
such outputs, all infinitely many C-residue zero spheres are concentric.
