# Project instantiation — derived from the completed action

Read `CURRENT_GATE1_MEMORY.md` and `references/geometric-register.md` first.
This file records semantic provenance and held order. It does not prescribe
unapproved Lean representations.

## 1. The action precedes the diagram

C1--C4 determine the vertically integrated distinguished
function/Möbius element. For an input `s ∈ 𝕆*`, its normalization selects
the sphere and coordinate where that element acts.

Orbit--stabilizer and `G₂` make the same action functorial on projective
frames and throughout `SphereWorld`.

At frame `X`, the native groupoid produced by that action is:

```lean
Grpd.of
  (AsectionActionStateWorld A (projectiveObjectFrame A X)).
```

Only afterward does `AsectionActionDiagram A` organize these
element-generated groupoids over the projective base. The ambient `Grpd`
classification is required later by the generic Grothendieck-components
engine; it is not the definition of the Gate-1 object. This construction is
not:

- the sectional projection `AsectionSlice : β ⥤ SphereWorld`;
- a constant `Grpd.of SphereWorld` wrapper;
- a fixed carrier decorated by A;
- an independently designed graph or product.

The native groupoid contains all intrinsic value states generated by the
one action positioned at `X`. The corresponding map of
`AsectionActionDiagram A` is the invertible transport of that whole action.
Downstream sections may abbreviate this exact live diagram as `F_A`.

## 2. The total

For the certified live diagram:

```text
T_A := TotalActionStateWorld A
     := Grothendieck (F_A ⋙ Grpd.forgetToCat).
```

An object remembers its projective base point and an action-generated value
state. A morphism remembers a base motion and the induced value transport.

The total records the already unified function/Möbius action. It does not
fuse two independently constructed natures.

This stage is certified by definitional equality to Mathlib's
`CategoryTheory.Grothendieck`. No project-local total structure or
compatibility alias is used.

## 3. The semantic C-residue inverse image

The semantic starting object is already certified:

```text
CResidueZeroLocus A = {z : ℂ | A.F z = 0 ∧ 0 < z.im}.
```

It is defined from the A-section's own equation. The sphere-zero
enumeration proves soundness, completeness, equality with the range, and
infinitude; it does not define the carrier. C4 therefore proves infinitude
and hence nonemptiness of the semantic locus, but nonemptiness is not a new
component-stage build step: the certified inverse image already has
inhabitants. C4 says nothing about the categorical one-component
calculation.

The next gate is the **residue-subdiagram gate**, with two receipts:

1. For each frame `X`, form and triple-certify
   `InverseImageCResidueStateWorldGroupoid A X`, the full inverse-image
   groupoid inside
   `Grpd.of (AsectionActionStateWorld A (projectiveObjectFrame A X))`.
2. For every `f : X ⟶ Y`, prove that the whole existing action transport
   carries the source inverse image into the target inverse image. Restrict
   that same transport to obtain the arrow map and its inclusion square.

The framewise inverse image is formed first. The preservation theorem does
not construct it; it licenses the assembly of those already-formed fibres
into a diagram. These are nevertheless the object and arrow parts of one
categorical gate.

The preservation proof uses the first-isomorphism/orbit--stabilizer theorem
in addition to the existing whole-action middle square and the uniqueness
of the residual north-stabilizer factor. It does not compare an “old value”
with a separately evaluated “new value”: there is one transported action
state, with all of its faces carried by the same square.

Quantification over every arrow already includes `f⁻¹`, so functoriality
supplies the inverse restricted transport; there is no separate “onto”
obligation.

The preservation proof must use the one action's existing middle squares.
`G₂` leaves the semantic complex coordinate definitionally fixed. The
projective leg is the distinguished disk action conjugated between frames,
with the stabilizer part uniquely forced by the fixed orbit
representatives. Input, positioned state, evaluated output, GPV lift, real
level, north leg, and sphere direction remain simultaneous faces of that
same square. The restricted square is derived from this theorem, never
designed separately.

Once the restricted diagram and its natural inclusion are certified,
`Grothendieck.map` supplies the total-level functor and
`functor_comp_forget` makes it a map over `𝓑`, with no hand-built arrow.
An unrestricted output-zero ambient system may be named later if a
downstream theorem genuinely requires it; it is not a prerequisite for the
C-residue subdiagram.

### The all-`t` arbitrary-frame supplier, and the exact remaining wiring

`reindexAsectionPresentation A f : AsectionPresentation A X → AsectionPresentation A Y`
(`ASectionFunctor.lean:835`) is the certified arbitrary-frame, all-`t` transport of the complete
GPV presentation. It carries `gpv` and `euler_gpv` **verbatim** — the tape, winding, levels and
uniqueness are the same fields, unmoved by the frame change — and reindexes only `toNorth`, instant
by instant, through `positionedOrbitSquare` at that instant's multiplier. `_id` (:848) and `_comp`
(:867) are green.

The exact boundary. `AsectionPresentation A X` and `F_A(X)` are indexed by the same `X`, but
`AsectionActionState` has `input`/`positioned`/`value` and **no presentation field**;
`reindexAsectionPresentation` transports the first, `AsectionActionTransport` the second, and **no
green theorem identifies the two transports**. The one attempted pairing was the Cartesian
`AsectionFiberType = state × presentation`, which is quarantined — pairing is not binding.

So the sole remaining theorem is a **comparison receipt between two existing manifestations of the
same distinguished element**: this is the proof content of `cResidue_preserved`, not an extra
preliminary theorem. It is not new geometry and not a second action. Once it exists,
`ObjectProperty.lift` and `liftCompιIso` close the gate mechanically.

## 4. The inherited north action and action-groupoid component collapse

The zero/N cone is inherited from the same distinguished element:

- it fixes projective `0` and `N`;
- its GPV action carries the north transport;
- orbit--stabilizer conjugates it in every projective frame;
- `G₂` makes it natural over all sphere directions.

No project functor is introduced merely to occupy the generic letter `K`,
and Lemma 8.3.4 is not a gate on the locked route. Once `𝓡_A` exists, its
total is recognized as the action groupoid already presented by the
first-isomorphism/orbit--stabilizer comparisons, the all-`t` middle square,
the inherited north action, and unique stabilizer factorization. This is
not a second action and not a new theorem that an independently assembled
system happens to be an orbit.

The already-built GPV north legs are genuine evidence for this stage. They
are read inside the derived residue system rather than substituted for it.
The certified inverse image already has inhabitants; C4 supplies the
stronger infinitude result. Remark 8.3.5 converts the resulting inhabited,
categorically one-component action groupoid into a singleton `π₀`. No
topological connectedness statement occurs.

## 5. Components and the descended real invariant

After the residue restriction has forced and certified its exact A-specific
diagram type, assign the exact A-specific component name and form its
component functor:

```text
[exact A-specific component name] :=
  π₀ ∘ [exact certified C-residue diagram].
```

Do not substitute a generic placeholder for either bracketed name before
that gate. The resulting category of elements is the dependent total of
exactly those chosen transported value states. The action-groupoid
recognition and Remark 8.3.5 first give the precisely named singleton
component carrier;
the el-identity gives

```text
π₀(el [exact A-specific component name])
  ≅ colim [exact A-specific component name],
```

and the project geometry has already proved that this precisely named
component carrier has one class. Do not abbreviate that carrier as `1`:
doing so hides the subsequent `val` reader that takes its unique class to
the real value used in the atomic conclusion.
Only then apply the separate generic equivalence, instantiated at the chosen
diagram:

```text
pi0GrothendieckEquiv (the certified C-residue diagram).
```

This supplies the comparison

```text
π₀(∫_𝓑 𝓡_A) ≃ colim_𝓑 (π₀ ∘ 𝓡_A) ≃ {k_A}.
```

It is not the source of the categorical one-component property: the action
groupoid and Remark 8.3.5 have already supplied the named singleton carrier.

The completed action already carries an `ℝ`-valued real-level function on
the residue states. The same naturality and action-groupoid calculation
prove that it is invariant under the relation being quotiented. It therefore descends
uniquely to `val_A` on the named component/colimit carrier.

The corresponding real-valued cocone is the categorical expression of this
orbit invariant. Its codomain is not a later choice: `ℝ` comes from the
real-level face being descended. The reader exposes compatible information
already carried by the value states; it installs no second register.

## 6. Held semantic order

```text
completed global action
  → derived F_A                       (certified)
  → F_A^Cat                           (same groupoids, viewed as categories)
  → Grothendieck.functor F_A^Cat = (π_A : T_A ⥤ 𝓑)   (certified)
  → CResidueZeroLocus A                              (certified)
  → InverseImageCResidueStateWorldGroupoid A X       (framewise receipt)
  → whole-action preservation for every base arrow   (arrow receipt)
  → the resulting C-residue diagram and inclusion
  → its Grothendieck total over 𝓑
  → recognize its orbit/coset/stabilizer presentation as the categorified
    orbit–stabilizer action already built
  → the existing inhabitant + categorical one-component action-groupoid
    calculation + Remark 8.3.5
    → the precisely named singleton component carrier and unique class
  → el-identity + pi0GrothendieckEquiv at the C-residue diagram
    → π₀(∫𝓡_A) ≃ colim(π₀ ∘ 𝓡_A) ≃ {k_A}
  → the already-compatible ℝ-valued real-level orbit invariant descends to val_A
  → c := val_A k_A and the atomic theorem.
```

Every remaining generic letter is replaced by an A-specific name and
triple-kernel receipt before the next generic theorem is instantiated. In
particular, the final component carrier and its unique class are named
explicitly; they are never abbreviated by `1`, so the final `val` step
remains visible.

## 7. A-specific names for the generic slots

| generic | A-specific | status |
|---|---|---|
| `F : 𝓑 ⥤ Grpd` | `AsectionActionDiagram A` | live |
| `F^Cat : 𝓑 ⥤ Cat` | `AsectionActionCatDiagram A` | live |
| `∫F` | `TotalActionStateWorld A` | live |
| `Grothendieck.functor F^Cat` (the over-base object) | *proposed*: `TotalActionStateWorldOverBase A` | not yet built |
| `Grothendieck.forget` | *proposed*: `totalActionStateProjection A` | not yet built |
| `Grothendieck.ι` (fibre inclusion) | — | unnamed |
| `F^res`, `P^res`, `el P^res` | — | read from the C3 face first, named after |

The underlying category `TotalActionStateWorld A` is the **domain** of the
over-base object, not a replacement for it: the value of the Grothendieck
functor is the pair (category, projection).
