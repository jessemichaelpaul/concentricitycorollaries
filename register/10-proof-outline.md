# The proof, end to end

Grounded in `Octonionic_RH_master.tex` and in the cited category theory
(`register/40-categorical-engine.md`). Where the settled register reads differently from the master,
the difference is recorded in §9 as a delta. **The master is not edited during the Lean endgame**;
the deltas are the record of what to reconcile afterwards.

## 1. The theorem

```lean
ASection.concentricity (A : ASection) :
  ∃ c : ℝ, ∀ n : ℕ, (A.sphereZero n).re = c
```

An A-section is a slice-preserving function on the compactified octonions `𝕆* = S⁸` satisfying
C1–C4. The Concentricity Theorem says its residue-`ℂ` zero 6-spheres are concentric. The octonionic
zeta is one A-section among many, and the Riemann Hypothesis is one of its corollaries
(master §Part 3 preamble, :815–821).

## 2. C1–C4 are inputs

- **C1** — meromorphic continuation through the real pole to the common north pole `N = ∞`.
- **C2** — the infinite Euler product, prime index internal.
- **C3** — the infinite Weierstrass product over the full divisor.
- **C4** — infinitude of the residue-`ℂ` zeros.

C4 makes the infinitely many zeros **intrinsic to the analytic object**, not a population installed
downstream. The realized zero population and its concentricity are outputs.

## 3. One distinguished element

C1–C3 determine a single distinguished element (master :1151–1156):

> the Euler product presents it at `0` and the Weierstrass product at `N`, the two boundary points it
> fixes, as a single diagonal Möbius element, through the commuting triangle `π∘E = exp`; per prime a
> centered degenerate family with one real level, each lifting uniquely to `N`.

The winding, lift-uniqueness, tameness, crossing, welding, and GPV transport laws are **readings of
that one action**, not twelve separate facts. The element is simultaneously a function in the
A-section ring and a Möbius group element; the green bridge is `eulerDiskAction_eq_value`
(`ProjectiveTransport.lean:218`) — the disk action's multiplier *is* `A.F z`.

Because the element is a function, its value states are inherited by evaluation. There is no
separate reader to install them.

## 4. Normalization is input to the element

For `s ∈ 𝕆*`, the normalization `(dir s, sliceCoord s)` selects the slice sphere and chart coordinate
**where the already-complete element is evaluated**:

```text
s ∈ 𝕆*  ↦  (dir s, sliceCoord s)  ↦  the sphere S²_(dir s) and its coordinate  ↦  A(s)
```

At real points and at `N` the slice direction is non-unique and irrelevant: those points are common
to every slice sphere and the realization is direction-blind there. This is why `N` is a shared
witness rather than a chosen one. *(Delta D1.)*

## 5. The global geometry

Orbit–stabilizer transports the same element through every projective frame, **objects and arrows
together**, and does so uniformly on every Riemann sphere — because the projective real circle and
the two fixed points `0`, `N` are common to all of them. `G₂` supplies the natural isomorphisms
between different sphere directions `I → J` in `SphereWorld`.

So orbit–stabilizer never has to manufacture the continuum of directions: the continuum is already
present, and `G₂` connects it. A slicewise proof is admissible only when it is uniform in an
arbitrary `I : SphereWorld` and immediately certified by `G₂` naturality. Choosing a `baseWorld` and
sweeping afterwards is a sectional projection, not the global action.

The identity and composition laws here are pure group algebra: every base arrow factors as
`f = orbitRep(Y) · stab(f) · orbitRep(X)⁻¹`, and `stabilizerPart_id` / `_comp` make the extension
functorial (master :1156–1158).

`AsectionSlice A : GreatCircle.Base ⥤ SphereWorld` is the **geometric projection** of this action
(master calls it `A^slice`, :1159). Its object map is constant by `rfl` —
`distinguishedWorldAction.obj I := I`, so `obj X = baseWorld` for every `X` — because the element
moves *inside* each Riemann sphere rather than relabelling the slice direction. Legitimate as a
projection; never the global action.

## 6. The element-generated groupoids and their diagram

At a projective frame `X`, put `m_X := projectiveObjectFrame A X`. The
`G₂`-equivariant realization of the same action produces the native
groupoid

```lean
Grpd.of (AsectionActionStateWorld A m_X).
```

This is the groupoid of all intrinsic value states generated by the one
action positioned at `X`. Only afterward does `AsectionActionDiagram A`
organize these groupoids over the projective great-circle base (master
:1160–1163). Its arrow maps are the invertible transports of the whole
element — orbit–stabilizer in the
projective/Möbius direction, GPV in the prime/winding direction, `G₂` in the sphere direction, and
input/output naturality joining the domain and codomain readings.

The ambient `Grpd` classification is required later by the literal
components equivalence (§8); it is not the provenance of the Gate-1
groupoid. Downstream prose may abbreviate the exact live diagram as `F_A`
only after the native groupoid above has been named.

Gate 1 is certified. Gate 2 audits its canonical Grothendieck total.

## 7. The total

```text
T_A = ∫_𝓑 F_A
```

An object remembers a projective footpoint and an action-generated value state; an arrow remembers a
base motion together with the induced value transport. The Grothendieck construction **records** the
already-unified function/Möbius action; it does not fuse two independently built natures
(master :1166).

## 8. The readout, in its settled order

Every type below is read from the completed action and its total, never predicted.

The semantic C-residue locus is already named and certified:
`CResidueZeroLocus A = {z : ℂ | A.F z = 0 ∧ 0 < z.im}`. It is the
A-section's own zero equation, not an indexed family installed in the
categorical construction. C4 proves that this locus is infinite (hence
nonempty); it says nothing about categorical connectedness.

1. **The residue-subdiagram gate has two receipts.**
   First, at every frame `X`, form and triple-certify the full inverse-image
   groupoid
   `InverseImageCResidueStateWorldGroupoid A X` inside `F_A(X)`.
   Second, prove for every base arrow `f : X ⟶ Y` that the whole existing
   action transport carries this inverse image into the target inverse image.
   Restriction then supplies the arrow map and the natural inclusion square.
   These are the object and arrow parts of one gate, not two competing
   constructions. Since the statement quantifies over every arrow, it also
   applies to `f⁻¹`; no separate “onto” gate is needed.
2. **The preservation theorem comes from the one element.** `G₂` leaves the
   complex coordinate definitionally fixed. The projective leg is the
   distinguished disk action conjugated between frames, with its stabilizer
   part uniquely forced by the fixed orbit representatives. The existing
   middle squares transport input, positioned state, evaluated output, GPV
   lift, real level, north leg, and sphere direction as simultaneous faces
   of that one action. The restricted square is derived from this
   preservation theorem; it is never independently designed.
3. **Name the orbit-theoretic objects before applying Riehl.** The residue
   diagram, its Grothendieck total, the orbit, stabilizer, coset, and
   distinguished component class must each receive an A-specific type and a
   triple-kernel receipt. Unique factorization through the stabilizer and the
   corresponding first-isomorphism comparison recognize the complete
   residue total as the action groupoid already built. This is the
   categorical one-component calculation. Topological connectedness is not
   at issue.
4. **Apply Remark 8.3.5 and the category-of-elements identity only to those
   certified A-specific objects.** Remark 8.3.5 turns the already-inhabited,
   categorically one-component residue action groupoid into its named
   singleton `π₀`; the categorical identities transport that class between
   the named presentations. C4 supplies infinitude and is not a separate
   nonemptiness step. Lemma 8.3.4 remains generic background and is not a
   project gate on this route. Never abbreviate the resulting carrier as
   `1`, because that hides the final value reader.
5. **Use the last comparison at the exact residue diagram.**
   `pi0GrothendieckEquiv` carries the already-proved singleton component
   carrier to the connected-component register of the residue total. It is
   not a source of connectedness.
6. **Descend the real orbit invariant.** The same distinguished action
   already carries an `ℝ`-valued real-level invariant. The action-groupoid
   calculation makes its constancy on the quotient relation explicit, so it descends
   uniquely to `val`. The corresponding cocone is a categorical expression
   of this invariant, not a choice of codomain. With the unique class named
   explicitly, `c := val κ` remains visible and the atomic conclusion
   follows.

The colimit consumes the **chosen** transport system. Never whole-total connectedness, never generic
zigzag invariance as the mechanism, never `Subsingleton (ConnectedComponents T_A)`.

## 8b. Why this is standard theory applied to a very specific object

Both levels of the construction are **literally action groupoids**, which is why the same textbook
statement is used twice.

| textbook (verbatim in `SOURCES/`) | the A-specific object |
|---|---|
| CTIC Ex. 2.1.iv — a subfunctor is subsets `Fc ⊂ Gc` such that **each `Gf` restricts** | the preservation composition, from the all-`t` conjugating square |
| CTIC Ex. 1.5.19, *"a categorification of the orbit-stabilizer theorem"* — `X//G`, components = orbits, automorphisms = stabilizers | **vertically**: `F_A(X) = AsectionActionStateWorld A m_X`, `G₂` on value states, `smul_coordinate = rfl` so one component per zero |
| the same, again | **horizontally**: `𝓑 = PGL(2,ℝ) ⋉ ℝ̂`, transitive by `orbitRep_spec`, `Aut(N) = NorthStabilizer`, factorization unique by `stabilizerPart_unique` |
| CTIC Ex. 2.4.10 — the category of elements of a `G`-set **is** the action groupoid | the two readings above are the same construction, not an analogy |
| CTIC Prop. 2.4.14 — `α : F ⇒ G` **is** a functor `∫α : ∫F → ∫G` over `C` | `Grothendieck.map ι_A : ∫𝓡_A ⥤ T_A`, over `𝓑` by `functor_comp_forget` |
| the el-identity inside 8.3.4's proof — `π₀(el X) ≅ colim X` | `pi0GrothendieckEquiv` at `𝓡_A`, its `Grpd`-valued extension |
| CHT Rem. 8.3.5 — non-empty and connected ⟺ `π₀` a singleton | the one class through which the existing `ℝ`-valued orbit invariant descends as `val` |

**What is generic and what is the author's.** Every theorem above is about a group, a set, an
invariant subset, an action groupoid, and `π₀`. None knows anything about zeta, octonions, or Euler
products. What is specific is *which* group, *which* set, and above all **why the subset is
invariant**: the acting element is simultaneously a Möbius group element and the function whose zero
set is the subset — `eulerDiskAction_eq_value`, the disk action's multiplier **is** `A.F z` — held at
every `t` by the GPV lift rather than only at endpoints.

An invariant subset is normally a hypothesis one assumes. Here it is forced, because the group and
the function are the same object. **That is why the ending is short.** Making the residue zero set
into an invariant subset of an action groupoid — through slice-preserving octonionic function theory,
C1–C4, the two presentations of one element, the prime-indexed lift through `N`, and orbit–stabilizer
across `SphereWorld` — *is* the theorem. A textbook lemma finishing it is the design working, not
evidence that something is missing.

## 8c. A stronger homotopical route, held

The orbit route is the canonical proof route because only `π₀` is needed.
Thomason's theorem supplies a stronger comparison:

```text
|hocolim N𝓡_A| ≃ B(∫𝓡_A).
```

The theorem and its provenance caveat are recorded in
`SOURCES/Thomason79.md`; the original paper was not fetched, so the
displayed statement is transcribed from Sharma's quotation. The required
nerve, bisimplicial diagonal, Theorems A/B, and homotopy-finality apparatus
is banked in `SOURCES/GJ.md`, `SOURCES/Quillen73.md`, and Riehl §8.5.

This homotopical route computes more than the component collapse used by
concentricity and requires additional simplicial infrastructure. It is a
post-Lean research route, not an alternative current gate. The almost
complex geometry of `S⁶`, while genuine, is not an input to Thomason's
nerve-and-diagonal comparison and remains non-load-bearing here.

## 9. Deltas from the master

Recorded, not corrected in the `.tex`.

| # | master | settled register |
|---|---|---|
| **D1** | :823–829 runs element → orbit–stabilizer → projection → lift, and is **silent** on normalization | normalized `s` is **input** to an already-complete element; it does not generate it (§4). A gap in the exposition, not an error |
| **D2** | :1176 "The colimit therefore consumes the entire transport system at once" | it consumes the **chosen** transport system — the zero/N system identified in §8, not everything at once |
| **D3** | :1167–1180 applies `pi0-grothendieck` **first**, then Riehl's identifications and singleton criterion | first certify the framewise C-residue inverse images and their restriction to a diagram; then name and certify the residue orbit/stabilizer/coset objects; recognize the residue total as the action groupoid already built, apply Remark 8.3.5 and the el-identity, and use `pi0GrothendieckEquiv` only as the last comparison |
| **D4** | :1177–1178 "the completed transports supply its categorical connectedness" | orbit–stabilizer, unique stabilizer factorization, the first-isomorphism comparison, GPV-to-`N`, and `G₂` supply the categorical one-component action-groupoid calculation; the certified inverse image supplies inhabitants and C4 supplies infinitude. No topological connectedness is asserted |
| **D5** | :1182–1187 the singleton class carries one real value already present in the normalized states | on the orbit route, the completed action already supplies an `ℝ`-valued real-level invariant. The action-groupoid quotient makes its descent to `val` canonical; the associated cocone expresses that invariant and does not leave the codomain ambiguous |
| **D6** | the endgame prose does not hold the orbit–stabilizer picture the whole way through, and reaches the singleton by the 8.3.4/finality route | **author's ruling 2026-07-26:** run orbit–stabilizer end to end and reach the singleton by **Remark 8.3.5**, which is cleaner for the one class and for the `val` step. The finality lemma is then needed only if a genuine north restriction is used. This is the largest remaining prose task, and it is **curation in the author's microhistory voice**, not a rewrite by a model |

**Curation task, post-Lean-phase.** D1–D6 are the reconciliation list for `Octonionic_RH_master.tex`. D6 is
structural: the last section should follow the same orbit–stabilizer thread it opens with, rather than
switching register at the readout. Also fix the stale Lean pin at `:997` (`AsectionFunctor`, now the
quarantined preflight; the live diagram is `AsectionActionDiagram A` and the total is
`TotalActionStateWorld A`). No `.tex` edits during the Lean endgame.

## 10. Corollaries

With `c` in hand, every populated residue-`ℂ` zero 6-sphere has centre `c`, so the infinitely many
populated zero spheres are concentric. The theorem does not prescribe the numerical value of `c`
(master :1146–1148). The octonionic zeta is an A-section, so the Riemann Hypothesis follows, along
with the infinitude of critical-line zeros — `Corollaries.lean`.

## 11. Terminal receipt

No `sorry`, `admit`, or `sorryAx`; axioms exactly `[propext, Classical.choice, Quot.sound]`.
